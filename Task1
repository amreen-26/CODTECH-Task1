int input = 9;

void setup() {
pinMode(input, OUTPUT);
}

void loop() {
digitalWrite(input,HIGH);       // turn the LED on (HIGH is the voltage level)
delay(500);                     // wait for a second
digitalWrite(input,LOW);        // turn the LED off by making the voltage LOW
delay(500);                     // wait for a second
}
